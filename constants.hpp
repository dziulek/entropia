#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

#include <utility>

float epsilon = 0.0001;
float defaultNumParticles = 100;
float defaultWidth = 200;
float defaultHeight = 200;
std::pair<float, float> defaultCorner = {10, 10};

#endif